package mn.univision.secretroom.presentation.screens.search

import androidx.compose.runtime.Composable
import androidx.tv.material3.Text

@Composable
fun SearchScreen(
    onBackPressed: () -> Unit,
) {
    // This is the main entry point for the search screen's UI.
    // You can set up your navigation and themes here.
    // For example, you might call a function to set up your navigation graph.

    // Placeholder for now
    // Replace with actual search UI components
    Text("Search Screen")
}