package mn.univision.secretroom.presentation.screens.home

import androidx.compose.runtime.Composable
import androidx.tv.material3.Text

@Composable
fun HomeScreen(
    onBackPressed: () -> Unit,
) {
    // This is the main entry point for the home screen's UI.
    // You can set up your navigation and themes here.
    // For example, you might call a function to set up your navigation graph.

    // Placeholder for now
    Text("Hello, Secret Room!")
}